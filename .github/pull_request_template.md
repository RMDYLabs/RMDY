## What changed

Describe the agent failure, the smallest intervention, and the supported runtimes.

## Evidence

- [ ] The reproduction is deterministic and privacy-safe.
- [ ] Regression fixtures fail before the patch and pass after it.
- [ ] `npm run db:verify`, `npm run test:cli`, `npm run lint`, and `npm run build` pass.
- [ ] No credentials, private traces, personal data, wallet keys, or validator keys are included.
- [ ] Documentation and compatibility notes are updated when behavior changes.
